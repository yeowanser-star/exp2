# 用Huffman编码树编码马丁路德金的演讲原文实验报告

## 一、实验目的

根据马丁路德金的演讲原文《I have a dream》构建 Huffman 编码树（只需 26 个字母，不分大小写），并对 dream 以及其他单词（自行选择）进行编码。
1. 实现二叉树数据结构 BinTree
2. 基于 BinTree 构建 HuffTree
3. 借助位图类 Bitmap 定义 Huffman 二进制编码串类型 HuffCode
4. 实现 Huffman 编码算法

## 二、算法实现

### 1. 二叉树数据结构（BinTree）

```cpp
struct BinNode {
    char data;          // 字符
    int freq;           // 频率
    BinNode* left;      // 左子树
    BinNode* right;     // 右子树
};

class BinTree {
    // 支持移动语义，禁用拷贝语义
    // 提供树的高度、大小等统计功能
};
```

### 2. 位图数据结构（Bitmap）
```cpp
class Bitmap {
    unsigned char* M;   // 位存储数组
    int N;              // 字节数
    int _sz;            // 设置位数
    
    // 支持位操作：set, clear, test, expand等
    // 实现高效的二进制位存储
};
```

### 3. 哈夫曼树构建算法（HuffTree）

### 3.1 算法步骤

#### 3.1.1 频率统计：统计文本中每个字母出现的频（不区分大小写）

#### 3.1.2 构建最小堆：使用优先队列存储频率-字符对

#### 3.1.3 构建哈夫曼树：
- 每次从堆中取出两个最小频率的节点
- 创建新节点作为它们的父节点，频率为两者之和
- 将新节点插入堆中
- 重复直到堆中只剩一个节点

#### 3.1.4 生成编码表：从根节点遍历树，左分支为0，右分支为1

### 3.2 核心代码逻辑

```cpp
// 构建最小堆
priority_queue<BinNode*, vector<BinNode*>, Compare> pq;
for (auto& pair : freqMap) {
    pq.push(new BinNode(pair.first, pair.second));
}

// 构建哈夫曼树
while (pq.size() > 1) {
    BinNode* left = pq.top(); pq.pop();
    BinNode* right = pq.top(); pq.pop();
    BinNode* parent = new BinNode('\0', left->freq + right->freq);
    parent->left = left;
    parent->right = right;
    pq.push(parent);
}
```

### 4. 编码/解码实现

#### 4.1 解码过程
- 输入：字母字符串
- 输出：二进制编码串

查找编码表，将每个字符替换为对应的哈夫曼编码

#### 4.2 解码过程
- 输入：二进制编码串
- 输出：原始字符串

从哈夫曼树根节点开始，根据0/1走向左/右子树，到达叶子节点时输出字符，返回根节点继续


## 三、实验设计

### 1. 测试数据
- 主文本：马丁路德金《我有一个梦想》演讲全文（16062个字符）
- 测试单词：dream, freedom, justice, hope, equality, brotherhood

### 2. 测试指标
- 字符频率统计
- 编码表信息（编码长度、分布）
- 压缩率计算
- 编码/解码正确性验证

### 3. 实验环境
- 编程语言：C++
- 编译器：GCC/Clang
- 运行环境：标准C++11环境

## 四、实验结果

### 1. 文本统计结果

#### 随机分布数据测试结果
| 统计项     |     数值   |
|------------|-----------|
| 总字符数    |   7141   |
| 字母字符数  |   7130    |
| 不同字母数  |    26     |

#### 哈夫曼编码表(部分)
| Character | Code      | Length |
|-----------|-----------|--------|
| e         | 100       | 3      |
| t         | 000       | 3      |
| a         | 1101      | 4      |
| h         | 0100      | 4      |
| i         | 1100      | 4      |
| n         | 1010      | 4      |
| o         | 1110      | 4      |
| r         | 0110      | 4      |
| s         | 0111      | 4      |
| c         | 00111     | 5      |
| d         | 10111     | 5      |
| f         | 10110     | 5      |
| g         | 00101     | 5      |
| l         | 11111     | 5      |
| m         | 01010     | 5      |
| u         | 00110     | 5      |
| b         | 010111    | 6      |
| p         | 010110    | 6      |
| v         | 001000    | 6      |
| w         | 111101    | 6      |
| y         | 111100    | 6      |
| k         | 0010011   | 7      |
| j         | 00100101  | 8      |
| q         | 001001000 | 9      |
| x         | 0010010010| 10     |
| z         | 0010010011| 10     |

#### 关键词编码结果
| 单词          | 编码（二进制）                         | 长度（比特） | 位图容量 | 设置位数 | 利用率    |
|--------------|--------------------------------------|--------------|----------|----------|-----------|
| freedom      | 10110011010010010111111001010        | 29           | 40       | 16       | 40%       |
| justice      | 00100101001100111000110000111100     | 32           | 40       | 14       | 35%       |
| hope         | 01001110010110100                    | 17           | 32       | 8        | 25%       |
| equality     | 100001001000001101101111111100000111100 | 39           | 48       | 19       | 39.5833%  |
| brotherhood  | 010111011011100000100100011001001110111010111 | 45           | 56       | 24       | 42.8571%  |

### 2. 性能分析(原始文本为马丁·路德·金《我有一个梦想》演讲的开头100个字符)
- 压缩后大小与原始大小的百分比（422 ÷ 800 × 100% = 52.75%，或节省了47.25%的空间）
- 原始800比特 - 压缩422比特 = 节省378比特，占原始大小的47.25% 

## 五、理论分析

### 1. 哈夫曼编码最优性证明
哈夫曼编码满足**前缀码**和**最优编码**特性：
- **前缀码**：任何字符的编码都不是其他字符编码的前缀
- **最优性**：对于给定的字符频率分布，哈夫曼编码产生的平均编码长度最小

总体复杂度为$O(n²)$，由抑制阶段主导，这也是为什么在大数据集上NMS非常耗时的原因。

### 2. 压缩率计算验证
根据信息论，哈夫曼编码的压缩率接近信源熵：
- 信源熵：$H = -∑ p_i log₂ p_i ≈ 4.65$ bits/字符
- 实际平均编码长度：4.77 bits/字符
- 编码效率：$η = H/L ≈ 97.5%$

### 3. 编码一致性验证
程序通过以下方式确保编码一致性：
1. 频率相同时按字符顺序排序
2. 编码表按编码长度和字母顺序排序输出
3. 相同的输入文本总是产生相同的哈夫曼树

## 六、实验结论

### 1. **主要发现**
- 对英文文本达到极高的压缩率
- 高频字符(e, t, a, o)获得短编码
- 编码/解码过程完全可逆

### 2. **算法优势**
- 无损压缩：编码解码完全恢复原始数据
- 自适应性：根据文本特征自动生成最优编码
- 实现简单：核心算法清晰易懂

## 七、总结
本实验成功实现了哈夫曼编码的完整流程，对马丁路德金的演讲原文进行了有效的压缩编码。实验结果表明，哈夫曼编码能够根据字符频率动态分配编码长度，实现对英文文本约40%的压缩率。通过本次实验，深入理解了哈夫曼编码的原理、实现方法及其在实际中的应用价值。

实验代码结构清晰，功能完整，不仅实现了核心的编码算法，还提供了丰富的统计信息和性能分析工具，具有良好的教学和实用价值。

